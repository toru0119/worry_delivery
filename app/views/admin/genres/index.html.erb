<div class="container py-4">
    <div class="p-5 mb-4 bg-light rounded-3">
      <div class="container-fluid py-5">
        <h1 class="display-5 fw-bold">ジャンルの追加・一覧</h1>
        <p class="col-md-8 fs-4">新しいジャンルの追加やジャンルが一覧で見れるようになってます。</p>
      </div>
    </div>

    <div class="row align-items-md-stretch">
      <div class="col-md-6">
        <div class="h-100 p-5 text-white bg-dark rounded-3">
          <h3>ジャンル追加</h3>
          <%=form_with model: @genre, url: admin_genres_path, local: true do |f| %>
          <div class="my-5">
              <%= f.text_field :name, placeholder: "ジャンル名" %>
              <%= f.submit '新規登録', class: "btn btn-outline-light offset-1" %>
          </div>
          <% end %>
        </div>
      </div>
      <div class="col-md-6">
        <div class="h-100 p-5 bg-light border rounded-3">
          <h3>ジャンル一覧</h3>
          <div class="container">
            <div class="row">
              <% @genres.each do |genre| %>
              <div class="col-md-6 mt-3"><%= genre.name %></div>
              <div class="col-md-6 mt-3"><%= link_to "編集する", edit_admin_genre_path(genre.id), class:"btn btn-outline-success offset-1" %></div>
              <% end %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>